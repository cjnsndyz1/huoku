# 货库·表达训练系统 — 用户体验与 UI 交互审计报告

| 项目 | 内容 |
|---|---|
| 审计日期 | 2026-08-31 |
| 审计人 | 鹏城信息AI专家（界面交互设计专家） |
| 审计范围 | 全站 6 页面（首页/记录/货库/回看/进步/设置）+ 设计系统 + 交互流程 |
| 方法 | Garrett 五层模型 + 尼尔森启发式 + WCAG 2.2 对照 + 代码走查 |
| 总体结论 | **底子很好（设计系统统一、范围克制、教练理念贯穿），总分 7.5/10；结构层存在 2 个 P0 级体验风险，需优先修复** |

---

## 一、TL;DR（先看这个）

**立即修（P0，数据安全与核心流程）：**
1. 🔴 货库删除**无确认、无撤销**，点一下垃圾桶=永久删云端数据
2. 🔴 记录页**草稿不持久化**，写一半刷新/切页即丢——对"本来就没货"的用户是毁灭性打击
3. 🔴 首页**缺"今天记了没"的决策引导**，北极星指标（连续天数）没有落在首页首屏

**建议修（P1，体验明显受损）：**
4. 🟠 全局按钮**无键盘焦点可见性**（WCAG 2.4.7）
5. 🟠 `--ink-faint (#9a9284)` 大量用于 12px 小字，**对比度仅 ~3.2:1**（标准 4.5:1）
6. 🟠 设置页 **API Key 明文显示**；登录/注册**无前置校验**
7. 🟠 货库/回看**空状态只有文字没有行动按钮**（应带 CTA）
8. 🟠 「今日三问」点击即把**问句原样填入**，未改直接保存会把问句存成一条货
9. 🟠 回看页**只能看不能记**，间隔复习闭环断裂（没有"今日新感悟"入口）

**锦上添花（P2）：** 加载骨架屏、保存成功反馈、跨年日期显示年份、回看页显示配图、PC 宽屏双列、移动端底部 Tab、标签筛选计数、设置页保存按钮防双击。

---

## 二、现状速览

| 维度 | 现状 |
|---|---|
| 产品形态 | 单人自用 Web 应用（PC + 移动响应式），React 19 + Vite + TS |
| 设计系统 | 「纸墨手账」：宣纸底 `#f6f1e7`、墨色 `#2b2620`、墨绿强调 `#3f5a43`、朱砂 `#b5442f`、楷体标题 — **统一、克制、有辨识度** |
| 页面 | 6 页：首页 / 记录(三步走) / 货库(缩略展开) / 回看(1/3/7/14/30) / 进步(打卡) / 设置 |
| 核心流程 | 记货（三行 + 标签 + 拍照）→ 货库 → AI 追问/套话检测 → 回看复习 → 打卡 |
| 好的交互设计 | 渐进式披露（三步走）、今日三问治白纸障碍、教练式提示词（只追问不代答）、AI 失败降级不影响记货、移动端触控目标基本 ≥44px |

---

## 三、五层模型体检明细

### 3.1 战略层（9.5/10）✅ 健康
- 定位清晰：治"没货"（感知+思考）而非"说不好"（口才）
- 北极星指标明确：连续挖货天数（streak），且有周活跃/累计数辅助
- 场景明确：日常造货/卡壳求助/回看复习/查看连续性

### 3.2 范围层（8.5/10）✅ 健康
- 功能克制无蔓延，P0-P2 划分合理，非目标清单明确（不做语音、不做评分、不做社交）
- 唯一提示：P2 拍照功能在移动端 `capture="environment"` 强制后置摄像头，部分 Android 机型 `accept="image/*"` 与 capture 组合会直接打开相机无法选图库，可考虑双入口。

### 3.3 结构层（6.0/10）🔴 需修复

| # | 问题 | 位置 | 影响 |
|---|---|---|---|
| S1 | **删除无确认、无撤销**：`remove()` 直接 `deleteEntry` 云端删除，无 confirm、无 undo、无 toast | `LibraryPage.tsx:242-250` | 误触=永久丢失一条货。对表达困难用户，每一条货都是挤出来的心血，删了就再也写不回来。**违反"破坏性操作必须有确认/撤销"** |
| S2 | **草稿不持久化**：三行内容只存 React state，无 localStorage 草稿。写一半刷新/误关/切页全丢 | `RecordPage.tsx:52-54` | 最打击造货动力的场景：好不容易挤出一行，被吞了。**建议防抖自动存草稿，进入页面恢复，保存成功清除** |
| S3 | **首页缺"今天记了没"决策引导**：PRD 用户流程第一步是"今天记了吗"，但首页只有一行小字 `streakText`，主按钮恒为"记一条货" | `HomePage.tsx:32-46` | 北极星指标是连续天数，首页却没把今日状态做成决策点。已记→该给正反馈/引导翻看；未记→强化 CTA。**首屏转化机会被浪费** |
| S4 | 回看/货库/进步/记录四页之间无横向跳转，只能回首页再进（深路径多一步） | `App.tsx` 路由 | 高频交替（记→看→再记）多一次往返。移动端可考虑底部 TabBar |

### 3.4 骨架层（7.0/10）🟠 中等

| # | 问题 | 位置 | 影响 |
|---|---|---|---|
| K1 | **空状态缺 CTA**：货库"还没有货。去记第一条吧。"只有文字无按钮；回看空状态同样 | `LibraryPage.tsx:289`、`ReviewPage.tsx:48` | 违反空状态规范（解释+行动+视觉），新用户到货库会"无处可去" |
| K2 | **回看闭环断裂**：回看页只展示 judgment+happened，无法记录"今日重温后的新想法"，也没配图 | `ReviewPage.tsx` | 间隔复习的核心价值=再看一眼→有新感悟→更新，现在只能看不能动 |
| K3 | **日期无年份**：`formatDate` 只显示 `MM-DD`，跨年条目无法区分 | `storage.ts:60-63` | 用满一年后货库会出现两个"08-22" |
| K4 | **今日三问防呆缺失**：点击问句直接 `setHappened(q)` 把问题当答案填入，不改就保存会存成一条以问句为内容的空货 | `RecordPage.tsx:186-188` | 货库出现"今天第一个跟你说话的人是谁？…"这类非记录内容 |
| K5 | 打卡日历 legend 写"绿色 = 那天挖了货"，实际是**墨绿**（accent #3f5a43），文案与视觉不符 | `ProgressPage.tsx:128` | 小瑕疵，改文案即可 |
| K6 | 记录页步骤条可任意跳转，但第 1、2 步"下一步"无内容校验（可空手进下一步，最后一步才校验） | `RecordPage.tsx:162-247` | 尚可接受（有意放松），但建议步骤条加完成态提示（已有 step-done） |

### 3.5 表层（6.5/10）🟠 中等（主要是无障碍）

| # | 问题 | 位置 | 影响 |
|---|---|---|---|
| V1 | **小字对比度不足**：`--ink-faint #9a9284` 用于 12px 小字（entry-date / cal-legend / entry-line-label / coach-hint / settings-note / entry-del 等），在宣纸底上对比度约 **3.2:1 < 4.5:1**（WCAG AA） | `index.css:15` 及多处使用 | 12px 小字本身难读，再加低对比度，阅读压力大。建议加深至 `#8a8275` 或直接改用 `--ink-muted` |
| V2 | **键盘焦点不可见**：仅 textarea/text-input 有 focus 样式，所有 button 无 `:focus-visible` | `index.css` | WCAG 2.4.7 不达标；键盘/辅助技术用户无法定位当前焦点 |
| V3 | **API Key 明文显示**：`type="text"`，输入时明文可见 | `SettingsPage.tsx:187-193` | 屏幕共享/旁人经过会泄露 Key；建议 `type="password"` + 显隐切换 |
| V4 | **登录/注册无前置校验**：邮箱格式、密码长度不校验直接调 Supabase，报错是英文原文 | `SettingsPage.tsx:55-83` | 新手困惑；建议前端校验 + 中文错误映射（"邮箱格式不对"等） |
| V5 | **移除图片按钮仅 26×26px**，远小于 44px 触控标准（Fitts 定律） | `index.css:591-605` | 移动端误触率高 |
| V6 | 加载态只有文字"加载中…"，首页 loading 时布局跳动（total-count 区域由空变有） | `HomePage.tsx:32-40` | 建议骨架屏或预留占位高度 |
| V7 | 保存成功无反馈，直接跳货库；错误提示有 WHAT 缺 HOW（"请检查云同步配置"没告诉用户去哪改） | `RecordPage.tsx:325` | 建议保存成功 1 秒"已存进货库"过渡 + 错误链接到设置 |

---

## 四、修复优先级清单（建议执行顺序）

### 批次 1 — P0（半天，数据安全 + 核心闭环）
1. **删除二次确认**：货库删除改为弹确认（或 5 秒 Undo 气泡），样式沿用纸墨风（朱砂警示）
2. **草稿持久化**：记录页三行 + 标签防抖写入 localStorage（key: `biaodaxunlian:draft`），进入页面恢复，保存成功清除
3. **首页今日引导**：今日已记 → 主按钮文案变"今天已记，再记一条 / 去看货库"，加一句鼓励；今日未记 → 保留"记一条货" + 若连续 ≥3 天显示"已连续 N 天，别断"

### 批次 2 — P1（一天，体验与无障碍）
4. 全局加 `:focus-visible` 焦点环（`outline: 2px solid var(--accent); outline-offset: 2px`）
5. `--ink-faint` 加深（#9a9284 → #8a8275），同步检查 12px 小字用例
6. API Key 输入改 `type="password"` + 眼睛切换按钮
7. 登录/注册前端校验（邮箱正则 + 密码 ≥6 位）+ 中文错误提示
8. 货库/回看空状态补 CTA 按钮（"去记第一条"跳 /record）
9. 今日三问防呆：点击后 placeholder 提示"换成你自己的话"，或保存时检测内容是否命中问题模板并二次确认
10. 回看页加"今日新想法"输入框（存 localStorage 或 entries 附加字段，P1 可先本地）

### 批次 3 — P2（机动，有空再做）
11. 加载骨架屏（首页/货库列表）
12. 保存成功 1 秒过渡反馈
13. `formatDate` 跨年时显示年份（当前年不显示）
14. 回看页显示配图（复用 `EntryImage`）
15. 移动端底部 TabBar（首页/记录/货库/回看/进步/设置）
16. 货库标签筛选显示数量角标
17. 设置页保存按钮加 `saving` 态防双击
18. 照片选择双入口（拍照/从相册选），不强制 capture

---

## 五、做得好的地方（别动）

- ✅ **设计系统统一克制**：纸墨手账气质完整落地，宣纸/墨色/朱砂/楷体一以贯之，无 AI 模板味
- ✅ **范围克制**：非目标清单明确，没有功能蔓延（Miller 定律 6 个入口 < 7）
- ✅ **渐进式披露**：三步走拆解"造货"流程，降低白纸恐惧
- ✅ **今日三问**：随机 3 个线索问句，精准治"打开页面不知道写什么"
- ✅ **教练式 AI 不代答**：提示词 + 代码双重约束（cleanCoachReply），产品理念贯穿
- ✅ **AI 失败降级**：AI 不可用不影响记货/翻看（PRD 验收标准达成）
- ✅ **错误处理有兜底**：60s 超时 + 重试 + 空响应重试，reasoning_content 不泄露给用户
- ✅ **触控目标基本达标**：主按钮/标签/删除键 ≥40-44px
- ✅ **文案有温度**：空状态、streakText（"别断"）、AI 提示都是人话，不是系统腔

---

## 六、一句话总结

> 战略、范围、视觉三层都很扎实——真正的问题都集中在**结构层的"数据安全感"**：删除要确认、草稿要留住、首页要说清"今天记了没"。修完这三个 P0，再把无障碍（对比度/焦点）和空状态补齐，这个产品就从"能用的自用工具"变成"愿意天天打开的习惯工具"。

---

*报告结束 · 由鹏城信息AI专家（界面交互设计专家）基于代码走查生成，可与 PRD 对照验收*
