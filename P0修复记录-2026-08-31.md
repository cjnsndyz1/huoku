# 货库 · P0/P1 修复记录

> 依据 `UX审计报告-2026-08-31.md` 执行：P0 聚焦「数据安全感」，P1 聚焦「无障碍与体验」。
> 日期：2026-08-31 · 状态：✅ P0+P1 全部完成，构建通过（tsc + vite build）

---

## P0-1 删除无确认无撤销 → 确认弹窗 + 撤销

| 项 | 说明 |
|---|---|
| 文件 | `src/pages/LibraryPage.tsx` + `src/index.css` |
| 问题 | 点垃圾桶立即删云端数据，误触 = 一条货永久消失 |
| 方案 | ① 删除前弹确认框（显示该条货的 snippet，杜绝误触）② 确认后底部 toast「已删掉 1 条货 + 撤销」，8 秒内可一键恢复 ③ 带图条目图片**延迟 10 秒**再真删，撤销即取消，避免"记录救回、图片没了" |
| 恢复原理 | `saveEntry` 是 upsert，同 id 重新写入即还原 |
| 无障碍 | 弹窗默认焦点在「留下它」（回车不会误删）、Esc/遮罩点击可关闭、role=dialog + aria-modal |
| 样式 | `.modal-mask/.modal/.modal-quote/.btn-danger/.toast/.toast-undo`，沿用纸墨 token（朱砂=危险、墨=toast 底） |

## P0-2 草稿不持久化 → localStorage 防抖草稿

| 项 | 说明 |
|---|---|
| 文件 | `src/pages/RecordPage.tsx` + `src/index.css` |
| 问题 | 写一半刷新/切页/误关，三行内容全丢——最打击造货动力 |
| 方案 | ① 三行文字 + 标签防抖 400ms 写入 `biaodaxunlian:record-draft` ② 进页面自动恢复，显示「上次写了一半的草稿已找回」提示条 ③ 提示条可「清掉」；文字全清空时提示条自动消失 ④ 保存成功即清除草稿 |
| 边界 | 配图是 Blob 无法持久化——恢复时若原草稿带图，提示「配图没保住，可重新拍一张」 |
| 降级 | 隐私模式/存储满时静默跳过，不影响正常记录 |

## P0-3 首页缺"今天记了没"引导 → 今日状态条 + 动态 CTA

| 项 | 说明 |
|---|---|
| 文件 | `src/pages/HomePage.tsx` + `src/index.css` |
| 问题 | 北极星指标是连续天数，却没落在首屏；主按钮恒为「记一条货」，无正反馈 |
| 方案 | 按 `stats.todayCount` 分两态：<br>• **未记**：朱砂虚线提醒条「今天还没挖，从一件小事开始」+ 主按钮「记一条货」<br>• **已记**：墨绿正反馈条「今天挖了 X 条 · 连续 N 天」+ 主按钮「再记一条」+ 次按钮「回看」（接复习闭环） |
| 意义 | 与 8/31 复盘「工具太好用反而零记录」同病灶——把「今天该不该记、记了没」说清楚，比加功能更能立习惯钩子 |

---

## P1 批次（无障碍 + 体验六连）

| # | 项 | 改动 | 验证 |
|---|---|---|---|
| P1-1 | 键盘焦点不可见 | `index.css` 全局 `:focus-visible`：2px 墨绿 outline + offset，覆盖按钮/链接/输入框 | WCAG 2.4.7 达标 |
| P1-2 | 淡墨对比度不足 | `--ink-faint #9a9284 → #756d5e`。实测原色在纸面上仅 **2.74:1**（审计估 3.2 还高估了）；新值 body 4.54:1 / 最浅纸面 4.82:1，12px 小字全部过 4.5:1 | node 计算验证 |
| P1-3 | API Key 明文 | `SettingsPage` 改 `type=password` + 眼睛显隐切换（lucide Eye/EyeOff，aria-label + title），`autoComplete=off` | 防偷窥 |
| P1-4 | 空状态无 CTA | 货库（含筛选态文案）+ 回看空状态，补「去记一条货」主按钮 | 新用户有路可走 |
| P1-5 | 今日三问把问句存成货 | `RecordPage` 点问句不再 `setHappened(q)`，改为**设为 placeholder + 自动聚焦输入框**，用户直接作答；文案改「挑一个问句，答在下面」 | 问句永远只是引导 |
| P1-6 | 回看只能看不能记 | `ReviewPage` 底部加「今日新想法」卡片：一句话输入 + 标签选择 + 「存成今天的货」（judgment 字段入库，date=今天），复习→产出闭环打通 | 保存后 refresh |

新增样式：`.key-field/.key-toggle`、`.empty-state`、`.new-idea/.btn-idea-save`，全部沿用纸墨 token。

**跳过项（说明）**：登录前置校验——`useEntries.needsSetup` 引导 + 保存 AI 配置时云端同步降级提示已覆盖大部分场景，该条需求边界不清，暂不做，避免过度设计。

## 验证

- `npm run build`：✅ tsc 无类型错误 + vite 构建通过（3s）
- 踩坑记录：React 19 的 `useRef` 必须传初始值，`useRef<number>()` 会报 TS2554，需写 `useRef<number | undefined>(undefined)`
- ✅ 505KB 单 chunk 警告已于 9/1 拆包修复（见下节）

## 性能优化：chunk 拆包（9/1）

| 项 | 说明 |
|---|---|
| 文件 | `vite.config.ts` → `build.rollupOptions.output.manualChunks` |
| 方案 | 四大块拆分：`react`（React + react-dom + react-router，首屏核心）、`supabase`（数据层）、`lucide`（图标）、`vendor`（其余 node_modules 兜底） |
| 效果 | 单文件 **505KB → 5 个 chunk**（业务 index 仅 41.6KB / gzip 13.6KB）；构建无体积警告；react/supabase 等大依赖几乎不变，**改业务代码只重下 41KB**，缓存命中率大幅提升 |
| 产物明细 | react 227KB(gz 72.6) · supabase 214KB(gz 56.0) · index 41.6KB(gz 13.6) · lucide 12.5KB(gz 3.0) · vendor 10KB(gz 3.5) |
| 注意 | `emptyOutDir: false` 使 dist 历史产物持续累积为孤儿文件（当前 ~2.8MB），可择机清理 |

## 性能优化：图片懒加载（9/1）

| 项 | 说明 |
|---|---|
| 文件 | `src/pages/LibraryPage.tsx`（EntryImage）+ `src/index.css` |
| 方案 | ① `IntersectionObserver`（rootMargin 200px）——**进视口前连 signed URL 请求都不发**，滚动到哪加载到哪 ② `loading="lazy" + decoding="async"` 双保险（兼作无 IO 环境的降级）③ 加载完成 `opacity` 淡入 0.3s（只动 opacity，符合动效约束） |
| 顺手修 | `.entry-image` 加 `aspect-ratio: 4/3`——原 max-height 无固定高度，懒加载空 src 时高度为 0，图片加载后会突然撑开 220px 造成滚动跳动 |
| 范围 | 只做货库列表图（可能很多张）；记录页预览是用户刚选的本地图、本就在视口内，不做 |
| 验证 | 构建通过；本次仅业务 chunk 与 css 哈希变化，react/supabase/lucide/vendor 缓存全部命中 |

## 维护：dist 孤儿清理 + emptyOutDir 根治（9/1）

| 项 | 说明 |
|---|---|
| 背景 | `emptyOutDir: false` 从不清理，历史构建累积 13 个孤儿文件（~2.8MB）；部署走 GitHub Actions（CI 全新构建后上传 dist），本地孤儿**不会被部署**，删除零风险 |
| 清理 | 删除 13 个孤儿（仅保留 index.html 引用的 6 个活文件），dist 2.8MB → **532KB** |
| 根治 | `vite.config.ts` 改 `emptyOutDir: true`（vite 默认），每次构建自动清空，不再累积 |
| 验证 | 构建后产物与清理前哈希完全一致——证明删掉的确实是孤儿，未破坏任何引用 |
| 未动 | `dist2/`（8/23 历史部署副本，不在 GH Pages 部署流程内），确认无用后可一并删除 |

## 下一步

功能面暂无明确 P2。剩余打磨项：图片懒加载（`loading="lazy"`）；dist 孤儿产物清理（确认部署流程后再动手）。
