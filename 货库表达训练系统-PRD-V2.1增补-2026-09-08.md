# 货库 · 表达训练系统 PRD V2.1 增补（提示词出口）

| 项目 | 内容 |
|---|---|
| 文档版本 | V2.1（增补） |
| 日期 | 2026-09-08 |
| 状态 | 待确认（确认后进入开发） |
| 基线 | 《货库表达训练系统-PRD-2026-08-23.md》V2.0 |
| 文档关系 | 增量增补：仅列出本次新增/修改项，**未提及的内容一律沿用 V2.0** |

> **V2.0 → V2.1 变更摘要**
> 1. 新增 **F10 语音提示词出口**：把一条货一键拼成提示词，复制到外部语音产品（豆包语音/视频对话）亲口开口；货库不自建实时语音。
> 2. F09 定位微调为「最低焦虑级打字演练」，顶部加「先说后打」一行；F09 不再承担"亲口说"的主通道职责。
> 3. 明确**三系统分工**：教材（妙搭手册）／训练场（货库）／对手（豆包语音 + 真实生活），划定货库内容边界。
> 4. 成功指标变更：北极星由「连续挖货天数 streak」改为「**周亲口出货条数**」，streak 降为护栏指标。
> 5. 给出 Now/Next/Later **阶段门 Roadmap** 与「首次亲口出货前冻结清单」。
>
> 本期**不新增数据表、不新增页面、不新增依赖、不新增路由**。

---

## §A 本次迭代要解决的问题（产品判断）

### A.1 现状与问题归类

V2.0 已实现「造货（F01-F03）→ 攒货（F02/F04）→ 出货（F09）」闭环并可正常使用，但"亲口说出去"这一最终结果尚未发生。按问题类型拆解，**没有一项是"能力不存在"**：

| 环节 | 现状 | 真实问题类型 | V2.1 处理 |
|---|---|---|---|
| 造货 | F01-F03 完整 | 内容质量问题（真货率、AI 代写残留），非功能缺失 | 本期不加功能，靠既有"只追问不代写"约束，来源标记留待 V3.0 |
| 出货 | F09 为打字通道 | 训练通道与目标不匹配：目标是"亲口说"，打字无法替代 | F10 提供通往外部语音的出口；F09 降级为打字演练 |
| 外显 | 刻意不做在产品内 | 流程协同断裂：缺一个把人"导出产品去开口"的动作 | F10 承担"导出"，V2.2 再做"轻回流" |

**结论**：V2.1 的价值是打通「选真货 → 去外部语音亲口说」这条路，把它从多步压缩到两步，而不是新增训练功能。

### A.2 关键决定（最新口径覆盖旧方案）

- **不自建实时语音/视频对练**。外部成熟产品（豆包语音/视频对话）已足够好，缺的只是提示词。任何"在货库内做语音识别、实时对话、AI 随机打断"的方案均不进入范围（同步写入 §9 非目标）。
- 货库只生产「与本人真货绑定的训练动作」，**不生产通用方法论内容**（话题库、绕口令、课程、模板讲解等留在妙搭教材，不搬进货库）。

### A.3 三系统分工（V2.1 起的产品边界）

| 系统 | 角色 | 生产什么 | 货库与它的关系 |
|---|---|---|---|
| 妙搭手册《提升口才的日常练习方法》 | 教材 | 场景模板、PREP、话题库、绕口令、排期、置信度说明 | 货库只**引用其结构**作为提示词变量，不复制内容 |
| **货库（本系统）** | 训练场 + 资产库 | 三行真货、AI 追问/套话判定、F09 打字演练、F10 出口、轻记录、间隔复习 | 唯一持续迭代对象 |
| 豆包语音/视频 + 真实生活 | 对手 | 实时语音对练、随机打断、对镜、一镜到底、真人场景 | 货库把人"推"到这里开口，结果回流一条轻记录（V2.2） |

---

## §B 本期范围

### B.1 做（In Scope）

1. **F10 语音提示词出口**：新增纯函数 `buildShipPrompt(entry, scenario, mode)` + 复制入口与反馈（详见 §C）。
2. **F09 文案微调**：F09 顶部加一行「先口头说一遍，再打出来」，引导先开口后打字。
3. **文档同步**：按 §D 对 V2.0 的功能清单、非目标、指标做局部修订。

### B.2 不做（Out of Scope，本期明确排除）

- ❌ 提示词历史记录、模板编辑器、多模板收藏、自定义模板
- ❌ 语音录入、语音转文字、实时语音/视频对话、AI 随机打断（用外部产品）
- ❌ 录音/波形/音视频存储
- ❌ 通用话题库、绕口令、方法论/课程内容（教材侧）
- ❌ 新增数据表、新增页面、新增第三方依赖
- ❌ 提示词多版本打磨：**首版只出一个版本，用满 5 次后才允许调整措辞**（防止打磨提示词变成新的建设点）

### B.3 首次亲口出货前冻结清单（产品宪法，优先级高于一切新需求）

在产生第 1 次"亲口出货 60 秒"之前，禁止：买录音/拍摄设备、写逐字稿、在货库加视频功能、排 30 天训练计划、在货库建话题库/方法论页、继续给妙搭教材加章节。冻结解除条件 = F10 被真实用于完成 ≥1 次亲口出货。

---

## §C F10 语音提示词出口（功能需求）

### C.1 用户故事

作为一个想练表达的人，我在货库展开一条真货时，希望一键拿到一段"已带好这条货和场景规则"的提示词，这样我无需自己组织要求，复制到豆包语音就能立刻开口。

### C.2 入口与流程

- 入口 1：P03 货库页 · 卡片展开态，在现有「说出来」（F09）旁新增并列按钮「复制语音提示词」。
- 入口 2：F09 出货练习面板顶部提供同一出口（打字前可改为去语音开口）。
- 流程：选场景（默认"向领导汇报"）→ 选模式（默认"温和"）→ 点复制 → 提示词写入剪贴板并显示成功提示「已复制，去豆包语音开口说 60 秒」→ 用户自行切到外部语音 App 粘贴开始。
- 复制后货库内**不弹出**可长期停留的编辑界面。

### C.3 场景与模式枚举

场景沿用 F09 四个并新增第 5 个「口头转述」（治"听→抓→组→说"卡顿，为最高频痛点）：

| key | 场景 | AI 扮演的听众 | 本轮目标 |
|---|---|---|---|
| report | 向领导汇报 | 直属领导 | 结论先行，30–60 秒说清进展/观点/请示 |
| chat | 跟朋友闲聊 | 朋友 | 自然讲清一件事，不端着、不汇报腔 |
| dinner | 饭桌即兴 | 饭桌上多人 | 被突然点名也能接住，讲得有意思 |
| convince | 说服别人 | 持不同意见的人 | 先认可对方，再给观点和依据 |
| retell | 口头转述（新增） | 没在场的同事/领导 | 听完一件事后抓重点、重组、口头转述清楚 |

模式两档：

| key | 模式 | AI 行为 |
|---|---|---|
| gentle | 温和（默认） | 让我完整说完一段再回应，停顿后才追问，降低开口压力 |
| stress | 压力 | 随时打断我，每 2–3 轮换一个我没准备的角度追问，模拟被点名 |

### C.4 `buildShipPrompt` 纯函数规格

- 位置：`src/services/shipPrompt.ts`，**纯函数、不调 AI、不花 token、不读写数据库、不依赖网络**。
- 输入：
  - `entry: HuoEntry`（取 happened / thought / judgment 三行，空行以"（未写）"占位但不阻塞生成）
  - `scenario: 'report' | 'chat' | 'dinner' | 'convince' | 'retell'`
  - `mode: 'gentle' | 'stress'`
- 输出：一段可直接粘贴的纯文本提示词（全文见 §C.5）。
- 规则：三行内容原样嵌入；不做改写、不做润色、不补全；任一行为空保留"（未写）"字样，提示用户这条货还不完整但仍可练。

### C.5 提示词成品模板（首版唯一版本，直接实现）

```text
你现在扮演我现实中的{场景角色}，我们用语音对话，你只能听我说、然后口头回应我，不要输出大段文字。

我要练的是把下面这件我真实经历的事，亲口说清楚：
- 发生了什么：{happened}
- 我当时怎么想：{thought}
- 我的一句话判断：{judgment}

你要遵守四条铁律：
1. 只做三件事：追问、用你自己的话复述我刚说的、点出我结构上哪里清楚/哪里绕；绝不替我说完整句子、绝不给我成稿、绝不给我打分。
2. {模式行为：温和=等我把一段说完、停下来后再开口追问；压力=可以随时打断我，每 2–3 轮换一个我没准备的角度追问。}
3. 如果我说"我卡了"，只给我一个关键词提示，不许给整句。
4. 等我讲完，用三句话收尾：哪里讲清楚了、哪里还绕、再追问我一个问题让我重说一遍那段。

{场景专属目标，见下表}现在开始：你先用这个身份，用一句很自然的口头话起个头，等我开口。
```

场景专属目标句：

- report：「我要练的是结论先行：第一句就给结论，再给理由和一个例子，最后说我需要你做什么。」
- chat：「我要练的是自然讲述，像真人聊天，不要汇报腔，允许有口语，但要有一条清楚的主线。」
- dinner：「我要练的是被点名后 3 秒内接住，先给一句观点再展开，讲得短、讲得有意思。」
- convince：「我要练的是先说出你立场里合理的部分，再补我的不同角度和依据，最后给一个可落地建议。」
- retell：「我会把我'听到的一件事'讲给你这个不在场的人听，你要追问我漏掉的关键信息（谁、做了什么、为什么、要我做什么），逼我先抓重点再按顺序重组，不许我照背原文。」

### C.6 交互与边界

- 复制用 `navigator.clipboard.writeText`，失败（非 HTTPS / 旧浏览器）时降级为「选中文本 + 长按/ Ctrl+C」的文本框兜底。
- 复制成功反馈 2 秒后自动消失，不阻断；断网状态下 F10 必须完全可用（纯前端）。
- 不记录复制历史、不上报、不落库。V2.1 阶段"复制后是否真开口"由用户本人自报。

---

## §D 对 V2.0 的同步修订（局部修改，不重写）

| V2.0 位置 | 修订动作 | 修订后 |
|---|---|---|
| §4 功能清单 | 新增一行 | F10 ｜ 语音提示词出口 ｜ P0 ｜ 纯函数拼提示词+一键复制，外接语音产品亲口练 ｜ P03 展开态/F09 面板 ｜ 纯前端（无接口） |
| §7.3 / F09 | 定位微调 | F09 = 最低焦虑级打字演练；面板顶部加「先口头说一遍，再打出来」；亲口说主通道移交 F10 + 外部语音 |
| §9 非目标 | 新增两条 | ❌ 自建实时语音/视频对练、AI 随机打断（外接豆包语音，货库只出提示词）；❌ 通用话题库/方法论/课程内容（教材在妙搭，不进货库） |
| §11 成功指标 | 改北极星 | 见 §G：北极星改为「周亲口出货条数」，streak 降为护栏 |
| §6 Page Map | 文字补充 | P03 承载功能追加 F10，不新增页面 |

> 其余章节（数据模型、外部集成、页面结构、F01-F08 验收等）保持 V2.0 原文不变；entries 表结构本期不动。

---

## §E 验收标准（Given / When / Then）

**F10 提示词出口**
- Given 一条三行非空的货，When 选择场景与模式并点「复制语音提示词」，Then 剪贴板得到完整提示词，三行内容正确嵌入、无 `{}` 占位符残留、场景角色与模式行为与所选一致。
- Given 某一行为空，When 生成提示词，Then 该行显示"（未写）"且仍可正常复制，不报错、不阻断。
- Given 切换到「口头转述 + 压力」，When 生成，Then 文本包含转述专属目标句与"随时打断"行为，且不含其他场景专属句。
- Given 断网，When 点复制，Then 功能照常可用（纯前端、无任何网络请求）。
- Given 剪贴板 API 不可用，When 点复制，Then 出现可选中文本的兜底方式，用户仍能拿到全文。
- Given 复制成功，When 反馈出现，Then 提示「去豆包语音开口说 60 秒」且货库不弹出新的可编辑页面。

**F09 文案**
- Given 进入 F09 面板，When 面板展示，Then 顶部出现「先口头说一遍，再打出来」一行，原有打字演练流程不变。

**回归（不得破坏 V2.0）**
- F01–F09 全部既有功能、entries 数据、登录与同步行为不受影响；无新增依赖、无新增数据表、无新增路由。

---

## §F Roadmap 与阶段门（Outcome-Driven）

> 用结果与行为门槛排序，不用日历排期；进入下一阶段的条件是真实"亲口出货"数据，不是功能完工。

| 阶段 | 目标结果 | 关键动作 | 进入/退出条件 | 重排触发器 |
|---|---|---|---|---|
| **Now · V2.1 提示词出口** | "选真货→去语音亲口说"降到两步，48h 内产生第 1 次语音出货 | F10 + F09 文案，详见 §C | 无门槛现在做；退出＝用它完成 ≥1 次 60 秒亲口出货 | 若复制 3 次仍未开口，说明卡点是焦虑不是摩擦，停止加功能，提前进入暴露阶梯训练 |
| **Next · V2.2 让开口可累积** | 开口从一次性事件变为可追踪轨迹 | ① ship_logs 轻表（entry_id/站次 0–5/日期/一句卡在哪，不存音视频，entries 主表不动）；② 提取模式：出货前只露 3 关键词、藏全文；③ 回看页"攒了没说过"筛选清死货 | 门：首次亲口出货 1 条；退出＝累计 10 条且 ≥1 次真人站 | 若用户抗拒记录站次，改为最轻的每日一条打卡 |
| **Later-1 · V2.3 降焦虑** | 能在越来越高压的真实场景开口 | 个人恐惧阶梯（自定义层级 + 主观焦虑 0–10，降到 3 分升阶）；即兴 60 秒发题器（货库只发题计时，录音用外部工具，不承接内容） | 门：累计 10 条 + 1 次真人 | 若阶梯导致回避，退回 V2.2 巩固，不叠加新机制 |
| **Later-2 · V3.0 表达资产化** | 货成为可复用的个人表达资产 | 判断谱系 + 来源标记（自己写/AI 协助/代写后已重写，收尾历史 AI 代写盘点）；先说后写的语音造货；PWA/离线/提醒等工程项 | 门：周均出货 ≥3 条、连续 4 周 | 依据 V2.2/V2.3 实际数据再定，不提前承诺 |

---

## §G 成功指标（V2.1 口径）

| 类型 | 指标 | 口径 |
|---|---|---|
| **北极星（新）** | 周亲口出货条数 | 走完「语音对练/对镜/录像/真人」任意外显站、本人自报并留一句复盘，即计 1 条；单人自用不做防作弊；V2.1 手工自报，V2.2 落 ship_logs |
| 护栏 | 连续挖货天数 streak | 仅防造货断流，**不再代表表达能力提升**（可被编套话/AI 代写刷高，故降级） |
| 护栏 | 累计造货数、周活跃天数 | 同 V2.0 |
| 验证假设 | 复制→开口转化 | V2.1 后观察"复制提示词次数"与"自报开口次数"的差距，手工记录，不埋点 |

---

## §H 风险与待验证假设

| 类型 | 内容 | 处理 |
|---|---|---|
| 待验证假设 | "提示词卡能显著降低开口摩擦"——无既有数据 | V2.1 后用"复制后是否真开口"验证；复制多次仍不说则按 §F 重排触发器改走焦虑训练，不继续优化提示词 |
| 行为风险 | 用"建设产品/打磨提示词"替代真实开口（历史上反复出现） | B.2 单版本用 5 次才许改 + B.3 冻结清单 + 阶段门三重约束 |
| 依赖 | 外部语音产品（豆包等）的可用性与交互变化 | 货库只产出纯文本提示词，不与其接口耦合，更换产品零成本 |
| 数据 | 自报出货可能虚高 | 单人自用、诚信自证，不做防作弊；以"是否真的更敢说"为最终主观校验 |

---

## §I 技术实现要点（供研发）

- 新增 `src/services/shipPrompt.ts`：导出 `buildShipPrompt(entry, scenario, mode)` 与场景/模式常量；与现有 `coachService.ts` 的 `SHIP_SCENARIOS` 复用同一套场景 key，新增 `retell`。
- 改动 `src/components/ShipPractice.tsx`（F09 顶部文案 + 复制入口）与货库卡片展开态（并列「复制语音提示词」按钮）。
- 复制：优先 `navigator.clipboard.writeText`，catch 后降级到只读文本框 + `select()`。
- 不新增 npm 依赖、不新增路由/页面、不改 entries 与 user_settings 表、不新增 Supabase 对象。
- 单元测试（对齐 V2.0 §13.4）：`buildShipPrompt` 的占位符替换、空行占位、五个场景 × 两种模式分支、输出无 `{}` 残留。
- 手工走查：货库展开 → 选「口头转述/压力」→ 复制 → 粘贴到外部语音 App → 完整开口一轮 → 回货库自报 1 条出货。

---

> 本增补经确认后：① 进入开发；② 与 V2.0 主 PRD 并存，开发完成后可合并为 V2.1 主文档。
