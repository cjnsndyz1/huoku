# 货库 · 二次 UI/UX 走查记录

> 在「移动端弹窗修复 + 全站走查」（见 `移动端UI修复记录-2026-09-08.md`）之后，针对 V2.1 新增模块（F09 出货练习 / F10 语音提示词 / 记录页 AI 挖掘流 / 云登录设置）及此前未深挖角落的二轮走查。
> 日期：2026-09-08 · 范围：全量源码 + CSS 1531 行 · 结论：**无 P0**（无数据安全 / 移动端裁切类新问题）

---

## 一、本轮新发现

### 🟠 A 组：确凿问题，改动小（4 项）

| # | 类型 | 位置 | 问题 | 建议修法 |
|---|---|---|---|---|
| A1 | **真 Bug** | `ShipPractice.tsx:86-92` + `VoicePromptModal.tsx:36-42` | **Esc 嵌套双关**：F09 弹窗内点「先口头说一遍」打开内嵌的语音提示词弹窗后，两层都注册了 window keydown Esc 监听——按 Esc 会把**语音弹窗和出货练习一起关掉**，练到一半的对话历史全没 | ShipPractice 的 Esc 处理器以 `voiceOpen` 为依赖，`voiceOpen` 时直接 return（Esc 交给子弹窗处理） |
| A2 | **Bug / 文案自相矛盾** | `supabase.ts:28-31` + `SettingsPage.tsx:71-84` | 开了邮箱验证时，注册实际成功但 `signUp` 抛「注册成功，请查收邮箱完成验证后，再回到这里登录」；UI catch 拼成「**注册失败：**注册成功，请查收邮箱…」——红字说失败、内容却是成功，用户会懵 | `signUp` 改为返回「成功但需验证」的结构化结果；UI 据此显示中性/成功态引导，不落错误分支 |
| A3 | 文案/体验 | `SettingsPage.tsx:65,80` | Supabase 英文错误原文直出：输错密码显示 `Invalid login credentials`、邮箱未验证显示 `Email not confirmed` | 做错误码 → 中文映射（「邮箱或密码不对」「邮箱还没验证，请先查收验证邮件」） |
| A4 | 文案 | `VoicePromptModal.tsx:150-152` | 自动复制失败兜底文案写「全选（Ctrl+A）后手动复制（Ctrl+C）」——**iOS 上没有 Ctrl**，移动端用户看到等于没用 | 改设备无关表述：「在预览框内点一下全选，再长按复制」 |

### 🟡 B 组：设计权衡，动前先聊（3 项）

| # | 类型 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| B1 | **跨天数据隐患**（最值得想） | `RecordPage.tsx` DRAFT_KEY | 草稿防抖存储**没有日期概念**：昨天的残稿，明天打开记录页仍显示「上次写了一半的草稿已找回」，顺手一保存就被记成**今天**——旧内容污染「今日已记」和连续天数，和首页北极星直接冲突。`savedAt` 已存了但没用 | 恢复提示带日期（「昨天写了一半的草稿」）；跨天时用警示样式，默认引导「清掉重记」，仍保留「继续写完」入口 |
| B2 | 无障碍 | 出货/语音弹窗 | 弹窗打开后焦点留在背景触发按钮上，键盘 Tab 可穿出遮罩进到页面（删除确认已做聚焦，这两个没做）；关闭后焦点也不归还 | 统一做：打开聚焦弹窗容器/首控件、关闭归还焦点（配合已有 :focus-visible 焦点环） |
| B3 | 体验 | `SettingsPage.tsx:240` | `msg` 单态共用 `.backup-msg`（accent 色）：**登录失败也显示成"成功绿"**，且测试连接/保存/登录的结果互相覆盖 | 拆 `{type:'ok'\|'err', text}` 双色渲染（可与 A2/A3 一并做） |

---

## 二、验证过没问题的（排除项，省得重复查）

- ✅ 删除撤销 toast 已有 `role="status"`（读屏可达）——我最初怀疑没加，实测已有，不动
- ✅ 触控目标：`.tag` / `.tq-btn` / `.btn` / 弹窗关闭钮全部 ≥40px min-height
- ✅ 删除确认：默认焦点在「留下它」（回车不会误删）、Esc/点遮罩可关、带图延迟 10s 真删可撤销
- ✅ 弹窗布局：ShipPractice 重构后固定区 ~200px + 滚动区，`.ship` 86vh / 小屏 92vh + 内滚，上一轮裁切问题已根治
- ✅ 教练铁律：DIG / CLICHE / SHIP 三条 system prompt 均守「只追问不代写」；`cleanCoachReply` 剥前缀/截断兜底齐全；重试 ×2 + 60s 超时 + resolveConfig 云端兜底健壮
- ✅ 挖掘流不重复问：`lastQuestion` 取 digRounds 尾部传参，prompt 明确禁重复；草稿含 digRounds，刷新不丢
- ✅ 今日三问（P1-5）：点击只设 placeholder + 聚焦，不再把问句存成货
- ✅ 设置页 API Key 密码框 + 显隐 + autoComplete=off（P1-3）；Supabase anon key 公开、RLS 按账号隔离，无密钥泄露面
- ✅ 未登录全站门禁 SetupGuide——设计决策（云端优先），非 bug

---

## 三、建议

1. **先手机实测**上一轮修复手感（F09 对话几轮 + 键盘弹出 + 删除撤销），确认无回归；
2. A 组 4 项（2 真 bug + 2 文案）改动极小，约半小时可落地，建议顺手做掉；
3. B1 跨天草稿涉及产品取舍（旧草稿自动清 vs 提示保留），拍板后与 B2/B3 一起做。

结论一句话：**这轮没有 P0，但 A1（Esc 双关）会在你嵌套用语音出口时实打实咬人，B1 会在第二天打开记录页时污染连续天数**——两个都是"你迟早会撞上"的坑，建议处理。

---

## 四、修复记录（A 组 4 项 + B1 + B3 · 2026-09-08 23:45 落地）

> 老板拍板「B1 一起做」后执行。构建通过（tsc + vite build，3.9s），缓存命中。

| # | 修复 | 改动 |
|---|---|---|
| A1 | Esc 双关关弹窗 | `ShipPractice.tsx`：Esc 监听在 `voiceOpen` 时直接 return，由子弹窗自行处理；deps 加 `voiceOpen`。按 Esc 只关语音提示词，不再连出货练习一起关 |
| A2 | 注册文案自相矛盾 | `supabase.ts`：`signUp` 不再把「需邮箱验证」当错误抛，改为返回 `'confirmed' \| 'verify-email'`；`SettingsPage.tsx` 对应分支显示「注册成功！请查收邮箱验证邮件」info 提示，不再红字「注册失败：注册成功…」 |
| A3 | Supabase 英文错误直出 | `supabase.ts` 新增 `authErrorText()`：Invalid login credentials / Email not confirmed / already registered / 密码长度 / 频率限制 / 网络 全部映射中文 |
| A4 | iOS 无 Ctrl | `VoicePromptModal.tsx` 兜底文案改「全选文字后复制（电脑 Ctrl+A / 手机长按全选）」 |
| B1 | 草稿跨天污染"今天" | `RecordPage.tsx` + `index.css`：恢复提示按 `savedAt` 判断——同天维持原样；跨天改**朱砂警示条**「这是 X 月 X 日写了一半的草稿——现在保存会记成『今天』。接着写，还是清掉？」，不自动删（数据安全感），用户自决 |
| B3 | 设置页失败显成功绿 | `SettingsPage.tsx` msg 单态改 `{kind:'ok'|'err'|'info', text}`；`.msg-err` 朱砂 / `.msg-info` 中性。测试连接失败、登录失败不再显示成墨绿 |

**遗留**：B2 弹窗焦点管理（出货/语音弹窗 Tab 可穿出遮罩）——建议作为无障碍专项单独排期。
