# 货库 · 移动端弹窗与 UI 走查修复记录

> 依据 2026-09-08 老板亲测反馈（「有些弹窗在移动端会显示不完整」）做全站 UI/移动端走查。
> 日期：2026-09-08 · 状态：✅ 修复完成，构建通过

---

## 根因定位

上轮修复（ce1bdb8）已把 F09 / 语音提示词弹窗改为「固定头 + 滚动体 + 固定底」，但 **F09 出货练习「对话中」状态漏了**：

### 🔴 ShipPractice 对话中（started）底部被裁切

| 项 | 说明 |
|---|---|
| 问题 | started 后 preBlock（提示语 + 语音出口按钮 + 5 个场景按钮 + 货预览 ≈ **270px**）整体平铺在 `.ship` flex column 里，**不可滚动、不可折叠**（场景已锁定，5 个按钮纯属摆设）；加上头部 56px + 输入区 ~100px + 底栏 ~45px，固定内容 ≈ **470px** |
| 触发 | 视口高 ≲650px（iPhone SE1 / 老安卓 / 分屏 / **手机软键盘弹出**）时，剩余空间 < 对话区 min-height:140px → 内容溢出被 `.ship overflow:hidden` 裁切 → **输入框与底部按钮跑出屏幕** |
| 修复 | 对话中重构为 `ship-body`（flex:1 + min-height:0 + overflow-y:auto）**统一滚动区**：顶部一条精简「向『领导』说 + 货预览」上下文条 + 消息流都进滚动区；输入区与底部按钮永远固定。**对话中不再重复渲染场景选择**（靠底部「换场景重来」切换） |

布局数学（修复后）：固定区仅剩 头 56 + 输入 100 + 底 45 ≈ 200px，667px 屏给滚动区 ~413px，568px 矮屏仍有 ~320px——任何对话长度都只滚中间，按钮永不裁切。

## 弹窗矮屏兜底（保险层）

| 项 | 改动 |
|---|---|
| 删除确认 `.modal` | 加 `max-height: min(480px, 80vh)` + `overflow-y: auto`——超长 snippet 在矮屏也只会内部滚动 |
| 语音提示词预览框 | `resize: vertical → none`——防桌面拉伸超出小屏滚动区设计 |
| 全弹窗 `.modal-mask` | 加 `overscroll-behavior: contain`——移动端滚动链条止步遮罩，不穿透滚到背后页面 |

## 全站移动端走查结论（非弹窗页面）

| 页面 | 结论 |
|---|---|
| 记录页（分步 + AI 问答流） | ✅ 纵向排布、无固定宽度；步骤条 flex:1、今日三问按钮 min-height 40 |
| 回看页（新想法卡片） | ✅ 纵向排布，textarea + tags + 按钮无固定宽度 |
| 进步页（统计 + 打卡历） | ✅ summary 3 列 / calendar 7 列均 grid 自适应，触控高度达标 |
| 货库（展开态按钮区） | ✅ `.entry-actions` flex-wrap，5 按钮自动换行 |
| 首页（今日引导条） | ✅ 状态条 + 按钮自适应 |

未发现其它窄屏溢出问题；480px / 500px 高度断点已覆盖既有场景。

## 验证

- `npm run build`：✅ tsc + vite 全过
- chunk 缓存：supabase/react/vendor 哈希未变（拆包收益持续有效）

## 文件改动

| 文件 | 改动 |
|---|---|
| `src/components/ShipPractice.tsx` | 对话中状态重构：preBlock 平铺 → ship-body 统一滚动 + ship-ctx 精简上下文条 |
| `src/index.css` | `.ship-body/.ship-ctx/.ship-ctx-role/.ship-ctx-entry` 新增；`.ship-log` 改随父滚动；`.modal` max-height+overflow；`.modal-mask` overscroll-behavior |
| `src/components/VoicePromptModal.tsx` | 预览框 resize:none |

## 待验证（手机实测）

- 打开 F09 → 开始对话 → 连说几轮 → **输入框与「换场景重来」是否始终完整可见**
- 软键盘弹出后输入仍可见、消息区可滚到底
- 删除一条 snippet 很长的货 → 弹窗内滚动、按钮不跑出屏幕
